{
  "name": "pokoin-firestore-rules-test",
  "private": true,
  "type": "module",
  "description": "Firestore security rules tests (emulator). Run: npm install && npm test",
  "scripts": {
    "test": "firebase emulators:exec --project pokoin-rules-test --only firestore \"node --test rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1"
  }
}
