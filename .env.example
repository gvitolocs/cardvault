# Pokoin / cardvault — copy to .env.honcho.local (gitignored). Never commit real keys.

# Honcho via peer1 Docker (Cursor MCP) — no Honcho Cloud billing required
HONCHO_API_KEY=local-dev-key
HONCHO_URL=http://127.0.0.1:18765
HONCHO_TUNNEL_LOCAL_PORT=18765
HONCHO_MCP_LOCAL_PORT=18787
HONCHO_WORKSPACE_ID=pokoin-cursor
HONCHO_USER_NAME=giuseppe
HONCHO_ASSISTANT_NAME=cursor
HONCHO_SESSION_LABEL=pokoin-coding

# Optional: Hermes flareon env (SSH keys for tunnel script)
# HERMES_ENV=/Users/giuseppe/Hermes/private/flareon/.env.local
